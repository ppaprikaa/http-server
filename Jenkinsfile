pipeline {
    agent {
        node {
            label "staging"
        }
    }

    stages {
        stage("Initial") {
            steps {
                echo "Hello"
            }
        }
    }
}
